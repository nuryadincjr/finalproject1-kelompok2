<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:id="@+id/homeMenu"
            android:icon="@drawable/ic_home"
            android:title="My To Do List"/>
    </group>

    <item android:title="Tools">
        <menu>
            <item android:id="@+id/archipMenu"
                android:icon="@drawable/ic_archip"
                android:title="Arsip"/>

            <item android:id="@+id/trashMenu"
                android:icon="@drawable/ic_trash"
                android:title="Sampah"/>
        </menu>
    </item>

    <item android:title="Other">
        <menu>
            <item android:id="@+id/settingsMenu"
                android:icon="@drawable/ic_baseline_settings"
                android:title="Pengaturan"/>

            <item android:id="@+id/aboutMenu"
                android:icon="@drawable/ic_baseline_info"
                android:title="Tentang"/>
        </menu>
    </item>

</menu>